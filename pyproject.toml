[project]
name = "red-button"
version = "0.1.0"
description = "Test which LLMs press the red button when explicitly told not to"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.50.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
    "matplotlib>=3.8.0",
    "tenacity>=8.2.0",
]
